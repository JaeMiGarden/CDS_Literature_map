// global
const HOME = "/home";

const USER = "/user";
const LOGIN = "/login";
const SiGNIN = "/signin";

const route = {
    home: HOME,
    
    user:USER,
    login : LOGIN,
    signin: SiGNIN,
};

export default route;